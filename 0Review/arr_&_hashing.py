# Input: nums = [2,7,11,15], target = 9
# Output: [0,1]
# Explanation: Because nums[0] + nums[1] == 9, we return [0, 1].



def two_sum(arr, tar):
    hash = {}
    res = []
    for n in arr:
        if():
            return 
hash[n] = tar - n
    print()


two_sum([2, 7, 11, 15], 9)


hash.get(n) is not None